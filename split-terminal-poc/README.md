# Split Terminal

### Setup 

* Install `tmux`
```bash
sudo apt update
sudo apt install -y tmux
tmux -V
```

### Output

* Works at terminator and ubuntu terminal
* ![terminator](terminator.png)
* ![ubuntu](ubuntu-terminal.png)
